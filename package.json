{
	"name": "tsns",
	"version": "1.0.0",
	"description": "tiny sns",
	"main": "app.ts",
	"scripts": {
		"test": "kill-port --port 5000 && mocha ./backend/test --recursive --exit",
		"dev": "kill-port --port 5000 && nodemon ./backend/app"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-validator": "^6.6.0",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.9.25"
	},
	"devDependencies": {
		"chai": "^4.2.0",
		"chai-http": "^4.3.0",
		"kill-port": "^1.6.1",
		"mocha": "^8.0.1",
		"nodemon": "^2.0.4"
	}
}
