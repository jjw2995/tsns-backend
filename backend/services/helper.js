// const { Joi, celebrate, Segments } = require("celebrate");

// // class validate {
// //   Segments = Segments;
// // }
// log = (...msg) => console.log(...msg);
// function validate(segment, fields = []) {
//   //   celebrate({[segment]: Joi.object().keys({})});
// }
// // [{Segments, [_id, name]},{Segments, [_id, name]}]
// // validate(body,[_id, describe])
// let a = new validate();
